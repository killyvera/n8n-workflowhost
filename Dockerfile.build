# Muelle / pull-only deploy — prebuilt private image (no monorepo compile).
# Full source build recipe lives in Dockerfile.build on this branch.
# Rebuild+push from master, then bump the tag below.
FROM ghcr.io/killyvera/n8n-workflowhost:1.0.0
