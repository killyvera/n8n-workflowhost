# BOTO product assets

Source: `boto-landing` (`public/brand/`, `public/favicon*.png`).

| File | Use |
|------|-----|
| `logo-mark.svg` | BOTO wordmark only, dark fill (light theme) |
| `logo-mark-word.svg` | BOTO wordmark only, light fill (dark theme) |
| `logo.svg` / `logo-light.svg` / `logo-dark.svg` | Full “BOTO TECHNOLOGIES” lockup (unused in UI) |
| `logo-mark.png` | Favicon / collapsed sidebar mark (transparent; themed via CSS mask) |

Do not overwrite upstream `@n8n/design-system` `N8nLogo` SVGs.
